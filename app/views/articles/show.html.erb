<div class="d-flex align-items-center border-bottom mb-3">
<h2 class="pr-2"><%= @article.title %></h2>
<%= link_to 'Edit', edit_article_path(@article.id), class: 'pr-2' %>
<%= link_to 'Delete',
            article_path(@article.id),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
</div>
<p><%= @article.content %></p>
